from setuptools import setup, find_packages

setup(
    name="optimatch",
    version="0.0.1",
    packages=find_packages(),
    install_requires=[
        # Proje bağımlılıklarını buraya yazın
    ],
)
